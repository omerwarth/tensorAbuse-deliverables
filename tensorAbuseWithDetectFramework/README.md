# TensorAbuse Project Entry Point

If you are here to run or understand the TensorDetect scanning workflows, start here:

- `TensorDetect/README.md`

Direct path:

- `/Users/owenmerwarth/Downloads/project_final/TensorAbuse/TensorDetect/README.md`

## What this gives you

The TensorDetect README explains:
- the **300-model batch scan workflow** (`tensordetect_workflow_300_model_batch_scan.py`)
- the **large-model hub scan workflow** (`tensordetect_workflow_large_model_hub_scan.py`)
- expected outputs and where results are written

## Quick navigation

From the TensorAbuse root:

```bash
cd TensorDetect
cat README.md
```
